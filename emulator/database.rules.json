{
	"rules": {
		".read": true,
		".write": true,
		"search_spells": { "$uid": { "results": { ".indexOn": ["name"] } } },
		"search_encounters": { "$uid": { "results": { "$campaignId": { ".indexOn": ["finished"] } } } },
		"campaign_notes": { "$uid": { "$campaignId": { ".indexOn": ["created"] } } }
	}
}
